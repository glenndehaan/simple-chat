import {h, Component} from 'preact';

export default class Chat extends Component {
    /**
     * Preact render function
     *
     * @returns {*}
     */
    render() {
        return (
            <div id="chat">
                Chat
            </div>
        );
    }
}
